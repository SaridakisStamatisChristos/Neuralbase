{{- if .Values.autoscaling.enabled -}}
{{- fail "autoscaling.enabled=true is unsupported: the chart does not reconcile StatefulSet replica changes through the Raft learner/joint-consensus membership protocol" -}}
{{- end -}}
