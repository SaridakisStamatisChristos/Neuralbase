{{- if .Values.autoscaling.enabled -}}
{{- fail "autoscaling.enabled=true is unsupported: NeuralBase currently uses fixed Raft membership and cannot safely change StatefulSet replica count through an HPA" -}}
{{- end -}}
