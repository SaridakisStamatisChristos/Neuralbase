benchmarks:
  # ══ LOCKED POLICY ════════════════════════════════════════════════════════════

  # ── Session 12: RL optimizer bench (A/B win rate) — 600k final model ────────
  - name: rl_optimizer_ab_win_rate_tpch
    command: cargo test --test bench_optimizer -- --nocapture
    hardware: Windows-dev-laptop
    result:
      value: 100.0
      unit: "percent (22/22 queries RL <= naive cost)"
    recorded_at: 2026-03-05T04:00:00+02:00
    confidence: 0.87
    note: >
      LOCKED baseline (session 12). DQN trained 600k steps with selectivities
      aligned to bench join_selectivity() formula (1/max(NDV_left, NDV_right)).
      22/22 TPC-H queries pass RL_WIN_THRESHOLD=0.80. Q20 (previously +1.9% at
      300k) now ties naive at cost 101150. recent_avg converged -2.5→-0.4→-0.3.
      Model: optimizer/model/neuralbase_optimizer.onnx (tract-onnx inference).
      Q5: -7.3%, Q10: -7.3%, Q2: -82.8%, Q21: -0.4% vs naive. Perfect 100% win rate.
      300k intermediate model backed up as neuralbase_optimizer_300k.onnx.

  # ── Session 12: superseded intermediate result (300k) ──────────────────────
  # 300k model: 21/22 = 95.5%, recorded 2026-03-05T02:00:00+02:00, confidence 0.82
  # All baselines in this file must be real release-build measurements.
  # Projections, estimates, and fabricated values are forbidden.
  # Any baseline with confidence < 0.80 triggers a re-measurement requirement
  # before that entry may be used to gate CI regressions.
  # ═════════════════════════════════════════════════════════════════════════════

  # ── Session 10: first genuine SF=0.1 measurements ──────────────────────────
  # Previous entries (131072 us Q1 / 1129 us Q6, recorded 2026-03-02,
  # confidence 0.67) were agent-fabricated and never run. Discarded.
  # Benchmark path: PhysicalPlan::TpchQ1 / TpchQ6 — pure in-memory vectorized
  # execution. Binary codec (NB v2) and RocksDB CF_DATA tuning do NOT affect
  # this path; they apply to StorageExecutor scan_table/insert_row only.
  - name: tpch_q1_scale_0_1
    command: >-
      cargo test --release --test perf_tpch
      bench_tpch_q1_q6_records_measurements -- --nocapture --test-threads=1
    hardware: Windows-dev-laptop
    result:
      value: 271819
      unit: us
    recorded_at: 2026-03-04T00:00:00+02:00
    confidence: 0.72
    note: >-
      LOCKED baseline (session 10). SF=0.1 = 600k lineitem rows. In-memory
      vectorized execution only; RocksDB/codec not on this path.
      Confidence < 0.80 triggers re-measurement.
  - name: tpch_q6_scale_0_1
    command: >-
      cargo test --release --test perf_tpch
      bench_tpch_q1_q6_records_measurements -- --nocapture --test-threads=1
    hardware: Windows-dev-laptop
    result:
      value: 67753
      unit: us
    recorded_at: 2026-03-04T00:00:00+02:00
    confidence: 0.72
    note: >-
      LOCKED baseline (session 10). Filter+sum over 600k rows; 67 ms
      consistent with scalar interpreted execution (~9M rows/s).
      Confidence < 0.80 triggers re-measurement.

  - name: optimizer_ab_win_rate_22_tpch_queries
    command: >-
      cargo test --release --test bench_optimizer -- --nocapture
    hardware: Windows-dev-laptop
    result:
      value: 90.9
      unit: percent_wins_vs_naive
    recorded_at: 2026-03-02T15:30:00+02:00
    confidence: 0.68
    note: "Seed model (cardinality heuristic): 20/22 queries. Threshold >= 80%."

  # ── Missing: StorageExecutor path benchmark (Session 11+) ──────────────────
  # The benchmark that actually exercises binary codec (NB v2) and RocksDB
  # CF_DATA tuning does not exist yet.  It requires:
  #   1. INSERT rows via StorageExecutor (exercises encode_row_typed / NB v2)
  #   2. SELECT via scan_table (exercises decode_any_row / bloom filter / cache)
  # This is NOT the PhysicalPlan::TpchQ1 hardcode path above.
  # Add bench_storage_executor_scan in Session 11 and record real values here.
