# NeuralBase standalone development configuration
# Not auto-loaded by the binary. Bash: set -a; . ./.env.example; set +a
NEURALBASE_LISTEN_ADDR=127.0.0.1:5432
NEURALBASE_DB_PATH=./data/neuralbase
NEURALBASE_METRICS_PORT=9090
NEURALBASE_AUTH_REQUIRED=false

# Standalone: writable local registry path.
# Clustered: this path is only a legacy migration source; the live authority is replicated RocksDB identity.
NEURALBASE_USERS_FILE=./data/users.json
# For clustered legacy migration, uncomment and set to sha256sum of the exact strict-SCRAM users.json selected as authoritative.
# NEURALBASE_IDENTITY_MIGRATION_SHA256=<64-hex-sha256>

# Optional connection admission controls
# Binary defaults: 100 global; unlimited per IP and per user.
# NEURALBASE_MAX_CONNECTIONS=1024
# NEURALBASE_MAX_CONNECTIONS_PER_IP=128
# NEURALBASE_MAX_CONNECTIONS_PER_USER=64

# Optional Raft configuration. Uncomment for a clustered topology.
# NEURALBASE_NODE_ID=node1
# NEURALBASE_RAFT_ADDR=0.0.0.0:7001
# NEURALBASE_PEERS=node2=127.0.0.1:7002,node3=127.0.0.1:7003
# NEURALBASE_RAFT_ELECTION_TIMEOUT_MS=150
# NEURALBASE_RAFT_TLS=0

# SQL TLS (requires a binary built with --features tls)
# SQL cert variables also enable TLS and cause plaintext startup rejection.
# TLS_CERT_PATH / TLS_KEY_PATH override NEURALBASE_TLS_CERT / KEY for SQL only.
# TLS_ENABLED=1
# NEURALBASE_TLS_CERT=./certs/server.crt
# NEURALBASE_TLS_KEY=./certs/server.key
# NEURALBASE_TLS_CA_CERT=./certs/ca.crt
# Raft mTLS additionally needs NEURALBASE_RAFT_TLS=1 and matching peer SANs.
# NEURALBASE_TLS_SERVER_NAME=neuralbase-node
# Full defaults and precedence: docs/CONFIGURATION.md
